import styled from 'styled-components';
import { SectionPadding } from '../../GlobalStyle';

const BlogSection = styled(SectionPadding)``;
const BlogWrapper = styled.div``;
export { BlogSection, BlogWrapper };

//
