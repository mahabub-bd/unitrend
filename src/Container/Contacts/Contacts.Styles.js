import styled from 'styled-components';
import { Paragraph1, SectionPadding } from '../../GlobalStyle';

const ContactsSection = styled(SectionPadding)``;
const Paragraph = styled(Paragraph1)``;

export { ContactsSection, Paragraph };

// export
